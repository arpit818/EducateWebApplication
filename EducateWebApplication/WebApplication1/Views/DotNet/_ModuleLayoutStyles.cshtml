<style>
    .module-page {
        max-width: 820px;
        margin: 0 auto;
        padding: 30px 20px;
        font-family: 'Segoe UI', sans-serif;
    }

    .module-crumb {
        font-size: 12px;
        color: #5c8a75;
        margin-bottom: 4px;
    }

    .module-heading {
        font-size: 24px;
        font-weight: 700;
        color: #1f4d38;
        margin-bottom: 20px;
    }

    .module-tabs {
        display: flex;
        gap: 20px;
        border-bottom: 2px solid #eef2f0;
        margin-bottom: 16px;
        font-size: 13px;
    }

    .tab {
        padding-bottom: 10px;
        color: #5c8a75;
        cursor: pointer;
    }

    .tab-active {
        color: #1f4d38;
        font-weight: 700;
        border-bottom: 2px solid #2f6e51;
        margin-bottom: -2px;
    }

    .back-link {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        color: #2f6e51;
        text-decoration: none;
        font-size: 13px;
        font-weight: 600;
        margin-bottom: 20px;
    }

        .back-link:hover {
            text-decoration: underline;
        }

    .module-section {
        border: 1px solid #d8e2dc;
        border-radius: 14px;
        margin-bottom: 14px;
        overflow: hidden;
    }

    .section-header {
        width: 100%;
        text-align: left;
        background: #f7fbf9;
        border: none;
        padding: 14px 18px;
        font-size: 14px;
        font-weight: 700;
        color: #1f4d38;
        cursor: pointer;
        display: flex;
        align-items: center;
    }

        .section-header i {
            margin-right: 8px;
            color: #5c8a75;
        }

    .item-row, .test-row, .exam-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 12px 18px 12px 40px;
        border-top: 1px solid #eef2f0;
    }

    .test-row, .exam-row {
        padding-left: 18px;
    }

    .item-left {
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 13px;
        color: #2b332f;
    }

    .pdf-icon {
        color: #b3261e;
    }

    .walk-icon {
        color: #2f6e51;
    }

    .test-name {
        font-size: 13px;
        font-weight: 600;
        color: #1f4d38;
    }

    .test-meta {
        font-size: 11px;
        color: #5c8a75;
        margin-top: 2px;
    }

    .pill {
        font-size: 11px;
        font-weight: 600;
        padding: 4px 12px;
        border-radius: 20px;
        white-space: nowrap;
    }

    .pill-done {
        background: #e6f4ec;
        color: #2f6e51;
    }

    .pill-pending {
        background: #f2f2f2;
        color: #888;
        cursor: pointer;
    }

    .pill-locked {
        background: #f2f2f2;
        color: #aaa;
    }

        .pill-locked i {
            margin-right: 4px;
        }
</style>
